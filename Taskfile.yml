version: '2'

tasks:

  generate:
    cmds:
      - protoc --go_out=plugins=grpc:. rpc/service.proto
    sources:
      - rpc/*.proto
    generates:
      - rpc/*.go

  clean:
    cmds:
      - rm -vrf grpcntt

  build:
    deps: [generate]
    cmds:
      - go build -o grpcntt
